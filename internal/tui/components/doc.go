// Package components is the reusable TUI component library (§10.4): gauge
// (harmonica-eased), sparkline/line chart (braille with an Apple_Terminal
// block-element fallback), stat tile (with a staleness badge), smart table (fed
// by the double-buffered snapshot), the frozen-plan confirmation modal, toast,
// spinner+progress, the onboarding/FDA panel, and the help overlay.
//
// The interface is colorblind-safe: every color signal is paired with an icon
// or a word (§10.6).
package components
